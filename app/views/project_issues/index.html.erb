<p>
  <h3>Project_name: <b><%= @project.project_name %></b></h3>
</p>

<table class="table table-striped">
  <tr>
    <th><h2><b>Issues</b></h2></th>
    <th colspan="3"></th>
  </tr>

  <% @project.issues.each do |a| %>
    <tr>
      <td><%= a.issue %></td>
      <td><%= link_to 'Destroy', project_project_issues_path(@project), method: :delete %></td>
    

    </tr>
  <% end %>
</table>

<h2>Add an Issue:</h2>
<%= form_with scope: :issue, url: project_project_issues_path(@project),  local: true do |form| %>  <p>
  <p>
    <%= form.label :issue %><br>
    <%= form.text_field :issue %>
  </p>
  
  <p>
    <%= form.submit %>
  </p>
<% end %>


<%= link_to 'Back', projects_path %>